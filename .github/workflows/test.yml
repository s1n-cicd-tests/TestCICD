name: GitHub Actions
on: 
  workflow_call:

jobs:
  init:
    runs-on: ubuntu-latest
    name: "init" 
    steps:
    - name: DL
      uses: dawidd6/action-download-artifact@v2
      with:
        repo: s1n-cicd-tests/Test2
        name: custom-artifact
        path: ./
        github_token: ${{ secrets.GITHUB_TOKEN }}
        search_artifacts: true
        workflow: test.yml
        branch: main
    - name: test
      run: cat artifact.txt
